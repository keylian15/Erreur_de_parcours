{
  "name": "todo-app",
  "private": true,
  "scripts": {
    "install": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "db": "mysql -u root -p < backend/src/database/schema.sql"

  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
